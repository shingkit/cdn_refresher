import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class ListItem extends StatelessWidget{

  const ListItem({Key key, String remark}){
    super(key: key);
  }
  @override
  Widget build(BuildContext context) {
    return Card(
      child: Text(),
    )
  }

}